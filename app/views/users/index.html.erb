<div class="row">
    <div class="col-xs-12">
        <div class="page-header">
        <h1>Listing tasks</h1>

        <table>
          <thead>
               <tr>
                <th>Name</th>
                <th>Email</th>
                <th>Since</th>
                <th>Action</th>
               </tr>
         </thead>
         
  <tbody>
    <% @user.each do |user| %>
      <tr>
         <td><%= user.name %></td>
         <td><%= user.email %></td>
         <td><%= user.created_at %></td>
         <td><%= link_to "Show", user_path (user) %></td>
         <td><%= link_to "Edit", edit_user_path(task) %></td>
         <td><%= link_to "Destroy", user, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

        
    </div>
</div>


